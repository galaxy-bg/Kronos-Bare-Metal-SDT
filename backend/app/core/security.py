def encrypt_secret_placeholder(value: str) -> str:
    # TODO: Replace with envelope encryption before storing production credentials.
    return value


def decrypt_secret_placeholder(value: str) -> str:
    # TODO: Replace with envelope decryption before storing production credentials.
    return value
